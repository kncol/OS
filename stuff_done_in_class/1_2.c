#include <stdio.h>

void main(){
    int height,width;
    printf("Type height of rectangle\n");
    scanf("%d", &height);
    printf("Type width of rectangle\n");
    scanf("%d", &width);
    printf("The area of the rectangl is: %d\n", height * width);
}